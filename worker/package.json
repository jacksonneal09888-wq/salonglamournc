{
  "name": "salon-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker API for Salon Glamour (Square proxy + stylist portal backend)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply salon-db",
    "d1:create": "wrangler d1 migrations create"
  },
  "dependencies": {
    "hono": "^4.3.7",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.50.0"
  }
}
